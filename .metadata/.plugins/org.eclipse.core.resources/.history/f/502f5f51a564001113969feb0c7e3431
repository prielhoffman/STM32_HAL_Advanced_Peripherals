#include "main.h"

/* Function prototype */
static void SystemClock_Config(void);

int main(void){
	  /*
	   * HAL_Init() initializes the HAL library.
	   *
	   * In bare-metal terms:
	   * this is not a peripheral init like USART/GPIO.
	   * It prepares the HAL infrastructure, especially the time base
	   * that HAL uses for delays and timeouts.
	   */
	  HAL_Init();

	  while (1){

	  }
}

void Error_Handler(void){
	  /*
	   * Disable interrupts and stay here if a serious error happens.
	   */
	  __disable_irq();

	  while (1){

	  }
}
